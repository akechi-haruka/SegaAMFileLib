# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net
name: .NET
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
jobs:
  build:
    runs-on: windows-2025-vs2026
    permissions: write-all
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: true
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore dependencies
        run: dotnet restore
      - name: Build
        run: dotnet build --no-restore
      - name: Test
        uses: zyborg/dotnet-tests-report@v1
        with:
         project_path: SegaAMFileTests
         report_name: SegaAMFileTests
         report_title: SegaAMFileLib Tests
         github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: Publish
        run: |
          dotnet publish .\SEGAAMFileLib\SEGAAMFileLib.csproj --configuration Release /p:PublishProfile=.\SEGAAMFileLib\Properties\PublishProfiles\Net10.pubxml -f:net10.0-windows10.0.18362.0
          dotnet publish .\SEGAAMFileCmd\SEGAAMFileCmd.csproj --configuration Release /p:PublishProfile=.\SEGAAMFileCmd\Properties\PublishProfiles\SegaAMFileCmd.pubxml
      - name: Upload Lib NET 10
        uses: actions/upload-artifact@v7
        with:
          name: SegaAMFileLib-net10-latest
          path: SEGAAMFileLib/publish/net10
          if-no-files-found: error
          overwrite: true
      - name: Upload Cmd
        uses: actions/upload-artifact@v7
        with:
          name: SegaAMFileCmd-latest
          path: SEGAAMFileCmd/publish
          if-no-files-found: error
          overwrite: true
              
